#include<iostream>
#include<vector>
using namespace std;

int main()
{
  int n, m;
  cin >> n >> m;
  vector<int> num;
  int t;
  for(int i = 0; i < n; i++)
    {
      cin >> t;
      num.push_back(t);
    }
  for(int & nu:num)
    cout << nu;
}
		  

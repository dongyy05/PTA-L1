#include<iostream>
#include<string>
using namespace std;

int main()
{
  string sen;
  int len = sen.length();
  vector<string>divi;
  while(len--)
    {
      cin >> sen;
      divi.push_back(sen);
    }
  int s = 0;
  for(auto int i = divi.end()-1; i <= divi.begin(); ++i)
    {
      if(s==0?"":" ")
	cout << *divi;
    }
}

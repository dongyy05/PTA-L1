#include<iostream>
#include<vector>
using namespace std;

int main()
{
  int n;
  vector<int>num;
  while(cin >> n)
    {
      num.emplace_back(n);
    }
  int a_1=0, a_2, a_3, a_4, a_5;
  int sum1 = 0;
  for(int i = 0; i < num.size(); i++)
    {
      if(num[i] % 2 == 0)
	{
	  
	  if(num[i] % 5 == 0)
	    a_1+=num[i];
	}
      else if(num[i] % 5 == 1)
	{
	  for(int i = 0; i < num.size()
    }
  cout << a_1;
}


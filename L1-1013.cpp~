#include<iostream>
#include<vector>
#define MAX 100005
int a[MAX];

void While(int n)
{
  a[0] = 1; a[1] = 1;
  for(int i = 2; i*i <= MAX; i++)
    {
      if(!a[i])
	for(int j = i*i; j <= MAX; j+=i)
	  a[j] = 1;
    }
  
